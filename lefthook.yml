pre-commit:
  commands:
    student:
      glob: "student/**/*.{ts,tsx}"
      root: "student/"
      run: "npm run lint"

    core:
      glob: "core/**/*.py"
      root: "core/"
      run: ". .venv/bin/activate && python dev.py lint"
