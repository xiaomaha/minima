pre-commit:
  commands:
    web:
      glob: "web/**/*.{ts,tsx}"
      root: "web/"
      run: "npm run lint"

    core:
      glob: "core/**/*.py"
      root: "core/"
      run: ". .venv/bin/activate && python dev.py lint"
